import { GroupproductsPipe } from './groupproducts.pipe';

describe('GroupproductsPipe', () => {
  it('create an instance', () => {
    const pipe = new GroupproductsPipe();
    expect(pipe).toBeTruthy();
  });
});

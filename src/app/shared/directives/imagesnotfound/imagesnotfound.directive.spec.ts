import { ImagesnotfoundDirective } from './imagesnotfound.directive';

describe('ImagesnotfoundDirective', () => {
  it('should create an instance', () => {
    const directive = new ImagesnotfoundDirective();
    expect(directive).toBeTruthy();
  });
});
